package view;

/* interface containing the methods in common between all the types of views used in the program */
public interface View {
	void start(); /*to initialize the window */
	void showMessage(String msg); /*to show a message in the window */
}
